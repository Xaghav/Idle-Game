extends TextureRect

export(Array) var stat :Array
export(String) var item_type:String ## only need to know when we uneqiup!! when comparing we know the slot type so we know item type
var rarity
func _ready():
	pass # Replace with function body.


# Called every frame. 'delta' is the elapsed time since the previous frame.
#func _process(delta):
#	pass
